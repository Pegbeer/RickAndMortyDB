query GetCharacterDetails($id:ID!){
    character(id: $id){
        id
        name
        status
        gender
        image
        origin {
            name
            dimension
        }
        created
    }
}